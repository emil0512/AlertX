apiVersion: v1
kind: Service
metadata:
  name: alertx-service
  labels:
    app: alertx
    app.kubernetes.io/name: alertx
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: alertx
